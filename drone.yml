kind: pipeline
name: default
steps:
 - name: build-docker-image
    image: react-simple
    err_ignore: true
    commands:
      - docker build -t react-simple .
    when:
      event:
        - push
      branch:
        - master